# User flow

<!--
Each step is what the user DOES or SEES — semantic, not implementation.

Good: "User clicks 'Pledge'. App shows confirmation modal with pledge amount.
       User confirms. App displays success state with charge summary."
Bad:  "Call /api/pledge with POST body {amount}. Render PledgeConfirmModal
       component. On confirm, dispatch PLEDGE_CONFIRMED Redux action."

Number the steps. Branches use sub-bullets. Failure modes are explicit.
-->

1.
2.
3.
