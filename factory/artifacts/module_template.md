<!-- This file is CURRENT-STATE-ONLY. Do not preserve past states. -->
<!-- One file per CURRENT module. The filename slug MUST match the module's path slug. -->

# Module: {{module_name}}

## Purpose

What this module is responsible for, one paragraph. Current behavior only.

## Key files

* `{{path/to/file.py}}` — what it does
* `{{path/to/other.py}}` — what it does

## Current behavior

How this module behaves today. If behavior changes, REWRITE this section to
the new truth — do not append "previously..." notes.

## Public interface

```
{{function_name(args) -> return_type}}
{{class_name}}
```

## Dependencies

* Internal: `{{module_name}}`, `{{module_name}}`
* External: `{{package_name}}`
