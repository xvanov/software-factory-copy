__pycache__/
*.pyc
*.pyo
.venv/
.env
.env.local
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/
state/*.db
state/*.db-journal
state/dry_run_scratch/
# Per-story event logs (JSONL, append-only, grow with chain activity).
# Surface via ``factory why`` / ``factory trace``; recreated on demand.
state/logs/
